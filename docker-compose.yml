version: '3.8'

services:
  oji-web:
    build: .
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - oji-network

networks:
  oji-network:
    driver: bridge

# Example with external AI service
# services:
#   oji-web:
#     build: .
#     ports:
#       - "8080:80"
#     depends_on:
#       - ai-service
#     networks:
#       - oji-network
#
#   ai-service:
#     image: your-ai-service:latest
#     ports:
#       - "9068:9068"
#     networks:
#       - oji-network
